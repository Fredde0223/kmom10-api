CREATE TABLE IF NOT EXISTS objects (
    gold INT DEFAULT 100,
    goldValue INT DEFAULT 0,
    silver INT DEFAULT 100,
    silverValue INT DEFAULT 0,
    copper INT DEFAULT 150,
    copperValue INT DEFAULT 0,
    iron INT DEFAULT 500,
    ironValue INT DEFAULT 0,
    aluminium INT DEFAULT 250,
    aluminiumValue INT DEFAULT 0
);
